--- crates/umbra-core/src/vector.rs
+++ replace + with - in <impl core::ops::Add for Vector3>::add
@@ -50,17 +50,17 @@
             z: self.z * s,
         }
     }
 }
 
 impl core::ops::Add for Vector3 {
     type Output = Vector3;
     fn add(self, o: Vector3) -> Vector3 {
-        Vector3::new(self.x + o.x, self.y + o.y, self.z + o.z)
+        Vector3::new(self.x + o.x, self.y - /* ~ changed by cargo-mutants ~ */ o.y, self.z + o.z)
     }
 }
 
 impl core::ops::Sub for Vector3 {
     type Output = Vector3;
     fn sub(self, o: Vector3) -> Vector3 {
         Vector3::new(self.x - o.x, self.y - o.y, self.z - o.z)
     }
