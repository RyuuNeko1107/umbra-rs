--- crates/umbra-core/src/solver.rs
+++ replace < with <= in minimize_golden
@@ -104,17 +104,17 @@
     F: FnMut(f64) -> f64,
 {
     let gr = (5.0_f64.sqrt() - 1.0) / 2.0; // ≈0.618
     let mut c = b - gr * (b - a);
     let mut d = a + gr * (b - a);
     let mut fc = f(c);
     let mut fd = f(d);
     for _ in 0..max_iter {
-        if (b - a).abs() < tol {
+        if (b - a).abs() <= /* ~ changed by cargo-mutants ~ */ tol {
             break;
         }
         if fc < fd {
             b = d;
             d = c;
             fd = fc;
             c = b - gr * (b - a);
             fc = f(c);
