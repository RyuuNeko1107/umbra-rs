--- crates/umbra-core/src/matrix.rs
+++ replace * with + in Matrix3::mul_vec
@@ -47,17 +47,17 @@
         Matrix3::new([[c, s, 0.0], [-s, c, 0.0], [0.0, 0.0, 1.0]])
     }
 
     /// 行列 × ベクトル。
     pub fn mul_vec(&self, v: Vector3) -> Vector3 {
         let r = &self.rows;
         Vector3::new(
             r[0][0] * v.x + r[0][1] * v.y + r[0][2] * v.z,
-            r[1][0] * v.x + r[1][1] * v.y + r[1][2] * v.z,
+            r[1][0] * v.x + r[1][1] + /* ~ changed by cargo-mutants ~ */ v.y + r[1][2] * v.z,
             r[2][0] * v.x + r[2][1] * v.y + r[2][2] * v.z,
         )
     }
 
     /// 行列 × 行列（self を後段に適用: `self * other`）。
     pub fn mul_mat(&self, other: &Matrix3) -> Matrix3 {
         let mut out = [[0.0; 3]; 3];
         for (i, row) in out.iter_mut().enumerate() {
