--- crates/umbra-core/src/julian.rs
+++ replace - with + in JulianDate2::normalized
@@ -30,17 +30,17 @@
         .normalized()
     }
 
     /// `part2` を `[-0.5, 0.5)` に寄せ、整数分を `part1` へ移す。
     pub fn normalized(self) -> Self {
         let extra = self.part2.round();
         JulianDate2 {
             part1: self.part1 + extra,
-            part2: self.part2 - extra,
+            part2: self.part2 + /* ~ changed by cargo-mutants ~ */ extra,
         }
     }
 
     /// 合計 JD（表示・粗い比較用。精度クリティカルな差分には使わない）。
     pub fn jd(self) -> f64 {
         self.part1 + self.part2
     }
 
