--- crates/umbra-core/src/solver.rs
+++ replace < with > in minimize_golden
@@ -107,17 +107,17 @@
     let mut c = b - gr * (b - a);
     let mut d = a + gr * (b - a);
     let mut fc = f(c);
     let mut fd = f(d);
     for _ in 0..max_iter {
         if (b - a).abs() < tol {
             break;
         }
-        if fc < fd {
+        if fc > /* ~ changed by cargo-mutants ~ */ fd {
             b = d;
             d = c;
             fd = fc;
             c = b - gr * (b - a);
             fc = f(c);
         } else {
             a = c;
             c = d;
