--- crates/umbra-core/src/solver.rs
+++ replace - with + in minimize_golden
@@ -117,17 +117,17 @@
             d = c;
             fd = fc;
             c = b - gr * (b - a);
             fc = f(c);
         } else {
             a = c;
             c = d;
             fc = fd;
-            d = a + gr * (b - a);
+            d = a + gr * (b + /* ~ changed by cargo-mutants ~ */ a);
             fd = f(d);
         }
     }
     (a + b) / 2.0
 }
 
 #[cfg(test)]
 mod tests {
