--- crates/umbra-core/src/vector.rs
+++ replace * with / in Vector3::scale
@@ -40,17 +40,17 @@
     /// ノルム（ユークリッド長）。
     pub fn norm(self) -> f64 {
         self.dot(self).sqrt()
     }
 
     /// スカラ倍。
     pub fn scale(self, s: f64) -> Vector3 {
         Vector3 {
-            x: self.x * s,
+            x: self.x / /* ~ changed by cargo-mutants ~ */ s,
             y: self.y * s,
             z: self.z * s,
         }
     }
 }
 
 impl core::ops::Add for Vector3 {
     type Output = Vector3;
