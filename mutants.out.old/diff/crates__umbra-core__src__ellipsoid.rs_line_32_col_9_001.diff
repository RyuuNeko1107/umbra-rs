--- crates/umbra-core/src/ellipsoid.rs
+++ replace Ellipsoid::e2 -> f64 with 1.0
@@ -24,17 +24,17 @@
 
     /// 短半径 b = a(1 − f) \[m\]。
     pub fn b_m(&self) -> f64 {
         self.a_m * (1.0 - self.f)
     }
 
     /// 第一離心率の二乗 e² = f(2 − f)。
     pub fn e2(&self) -> f64 {
-        self.f * (2.0 - self.f)
+        1.0 /* ~ changed by cargo-mutants ~ */
     }
 }
 
 /// 観測者の扁平補正済み地心動径成分（単位: 地球赤道半径 Re）。
 ///
 /// ρsinφ′・ρcosφ′（Meeus *Astronomical Algorithms* Ch.11）。視差・ベッセル観測者射影で使う。
 #[derive(Debug, Clone, Copy, PartialEq)]
 pub struct GeocentricObserver {
