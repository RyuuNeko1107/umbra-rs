--- crates/umbra-core/src/vector.rs
+++ replace - with / in <impl core::ops::Sub for Vector3>::sub
@@ -57,17 +57,17 @@
     fn add(self, o: Vector3) -> Vector3 {
         Vector3::new(self.x + o.x, self.y + o.y, self.z + o.z)
     }
 }
 
 impl core::ops::Sub for Vector3 {
     type Output = Vector3;
     fn sub(self, o: Vector3) -> Vector3 {
-        Vector3::new(self.x - o.x, self.y - o.y, self.z - o.z)
+        Vector3::new(self.x - o.x, self.y - o.y, self.z / /* ~ changed by cargo-mutants ~ */ o.z)
     }
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
 
     #[test]
