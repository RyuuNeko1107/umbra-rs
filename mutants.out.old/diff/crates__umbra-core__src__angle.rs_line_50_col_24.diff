--- crates/umbra-core/src/angle.rs
+++ replace * with + in Degrees::to_radians
@@ -42,17 +42,17 @@
 impl Degrees {
     /// 値を包む。
     pub const fn new(value: f64) -> Self {
         Degrees(value)
     }
 
     /// ラジアンへ変換。
     pub fn to_radians(self) -> Radians {
-        Radians(self.0 * PI / 180.0)
+        Radians(self.0 + /* ~ changed by cargo-mutants ~ */ PI / 180.0)
     }
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
 
     const EPS: f64 = 1e-12;
