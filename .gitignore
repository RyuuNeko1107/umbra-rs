# Rust
/target
**/target

# cargo-mutants 出力（ミューテーション検証, 工程7）
/mutants.out
**/mutants.out

# 私的な設計原本（ずんだー等の内部用途を含むため非公開）
*.docx

# エディタ/ハーネス
.claude/
.DS_Store
Thumbs.db
*.swp

# Cargo.lock はワークスペースにバイナリ(cli)を含むため追跡する
