//! `umbra` CLI — work in progress。
//!
//! サブコマンド（search / local / path / bessel / inspect / validate）は Milestone 7+ で実装
//! （`docs/issues/ISSUE-031`/`ISSUE-032`、`docs/algorithms/`）。

fn main() {
    println!("umbra-rs CLI — work in progress. See docs/ for design.");
}
